package io.nurudeenlawal;

/**
 * Created by nurudeenlawal on 5/1/16.
 */

/**
 * This is the ENUM class that is built to hadle the commands
 */
public enum  Ops { ADD, SUB, MULT, DIV, SQR, CUBE, TOEXP, ROOT, INV, SIN, COS, TAN, INSIN, INCOS, INTAN, LOG, INLOG, NATLOG, INNATLOG, TOTALRECALL, EXIT, MEM, CLEAR}
